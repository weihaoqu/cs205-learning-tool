DATABASE_URL="postgresql://cs205user:CHANGE_ME@db:5432/cs205?schema=public"
JWT_SECRET="CHANGE_ME_MIN_32_CHARS"

# Comma-separated reserved admin addresses. Public registration REFUSES these;
# admin accounts are created only by scripts/create-admin.mjs.
ADMIN_EMAILS="wqu@monmouth.edu,rkamp@monmouth.edu"

# Cohort that new student registrations join. Must match YYYY-(spring|fall|summer).
CURRENT_SEMESTER="2026-fall"
